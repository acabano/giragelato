# giragelato
La ruota della fortuna

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Configure with admin adminpassword
4. Test with testuser password123
